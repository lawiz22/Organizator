# Dépendances DJI Organizator — auto-suffisantes (n'importe PAS la stack
# torch/CUDA de requirements.txt utilisée par MediaMind AI).
#
# Installation :
#   .venv\Scripts\python.exe -m pip install -r requirements_dji.txt
# ou via UV :
#   .uv\uv.exe pip install -r requirements_dji.txt

# ── UI ────────────────────────────────────────────────────────────────────
nicegui>=3.0.0
pywebview>=5.0

# ── Métadonnées photos/vidéos DJI (EXIF/XMP complet via ExifTool) ────────
PyExifTool>=0.5.6

# ── Génération thumbnails photos ─────────────────────────────────────────
Pillow>=10.0.0

# ── Génération thumbnails vidéos (extraction 1 frame) ────────────────────
opencv-python>=4.9.0

# ── Corbeille système (Windows/macOS/Linux) ──────────────────────────────
send2trash>=1.8.3
