# Dépendances supplémentaires pour DJI_organizator
# (complète requirements.txt de MediaMind AI, utilise le même .venv)

# Wrapper Python pour ExifTool (lecture complète métadonnées DJI incl. XMP)
PyExifTool>=0.5.6

# Corbeille (Windows/macOS/Linux) — déjà installé côté organizator
send2trash>=1.8.3

# Image / vidéo thumbnails
Pillow>=10.0.0
opencv-python>=4.9.0
